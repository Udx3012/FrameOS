{
  "name": "frameos-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chat-native video engine — FrameOS pipeline. Idea/clip/YouTube link -> captioned vertical short.",
  "keywords": [
    "frameos",
    "ffmpeg",
    "video",
    "shorts",
    "reels",
    "telegram",
    "ai",
    "captions",
    "yt-dlp"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest",
    "gateway": "node --import tsx src/gateway/run.ts",
    "frameos": "node --import tsx src/cli.ts",
    "reely": "node --import tsx src/cli.ts"
  },
  "dependencies": {
    "convex": "^1.42.1",
    "dotenv": "^16.4.5",
    "p-limit": "^6.1.0",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
