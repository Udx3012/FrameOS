# reely — Hermes skill

Skill name: reely
What it does: turns a chat message (idea / raw clip / YouTube link) into a captioned vertical short.
Entry: src/gateway (Telegram gateway) -> src/router (NL -> JobSpec) -> src/queue (executor).
Capabilities used: gateway (messaging), skill (this), content generation (brainstorm/script/captions).
